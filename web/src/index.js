import React from 'react'; //necessário para utilizar tags HTML
import ReactDOM from 'react-dom'; //ReactDOM se comunica com o Documento HTML
import App from './App'; //

ReactDOM.render(<App />, document.getElementById('root')); //'root' é a div no /public/index.html
// JSX -> JAVASCRIPT + HTML Ex.: <App />
